# MOT20-YOLOv26 最小依赖安装 (仅缺失的核心依赖)
# 已过滤掉环境中已有的: torch, torchvision, numpy, pillow, pyyaml, tqdm, matplotlib, packaging

opencv-python>=4.5.0
scipy>=1.7.0
pandas>=1.3.0
seaborn>=0.11.0
scikit-learn>=1.0.0
filterpy>=1.4.5
lap>=0.4.0
Cython>=0.29.0

# 一键安装命令:
# pip install -r requirements_minimal.txt
